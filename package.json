{
  "name": "airpp-registry-mvp",
  "version": "0.3.0",
  "type": "module",
  "private": true,
  "description": "AIRPP Registry — public protocol registry, conformance sandbox and MCP provenance server",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/lib/validator.js && node --check src/lib/store.js && node --check src/routes/api.js",
    "test": "node tests/validator.test.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "nanoid": "^5.0.7",
    "nodemailer": "^6.9.15",
    "pg": "^8.12.0"
  }
}
